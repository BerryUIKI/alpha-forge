// Agent tool definitions — Phase 1 placeholder.
// Tool registry will be populated in later phases.

/// Stub for future tool capabilities.
pub struct ToolRegistry;

impl ToolRegistry {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ToolRegistry {
    fn default() -> Self {
        Self::new()
    }
}
